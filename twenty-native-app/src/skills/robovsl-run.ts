import { defineSkill } from 'twenty-sdk/define';

import { ROBOVSL_RUN_SKILL_UNIVERSAL_IDENTIFIER } from 'src/constants/universal-identifiers';

// Ported from local agent skill: robovsl-run/SKILL.md
export default defineSkill({
  universalIdentifier: ROBOVSL_RUN_SKILL_UNIVERSAL_IDENTIFIER,
  name: 'robovsl-run',
  label: 'Robovsl Run',
  description: 'Run a Robotic VSL walkthrough recording for a Twenty agencyProspect end to end: env preflight, forced+cropped Camoufox/Cap recording with cursor tracking, R2 upload, Twenty videoUrl attach, and verification that the video serves on its page. Load when recording a prospect walkthrough video or debugging a VSL run.',
  content: "# RoboVSL Run (prospect walkthrough → R2 → Twenty → page)\n\nRecords a human-like walkthrough (prospect's real site → generated redesign)\nwith cursor motion, uploads to R2, attaches to Twenty, and verifies the video\nserves on the prospect's page. Every failure below is LOUD by design — a dead\nrun reports FAILED with the reason, never a fake ATTACHED.\n\n## 0. Preconditions (check once per machine)\n\n- `uv`, `bun`, `ffmpeg` on PATH; Camoufox launches headed (needs an\n  interactive desktop — RDP/locked sessions break `humanize` mouse motion).\n- `C:\\Users\\0\\.buzz\\REPOS\\ui-kit\\.env.local` holds: `TWENTY_BASE_URL`,\n  `TWENTY_API_KEY`, `GROQ_API_KEY`, `FRONTEND_BASE_URL`, R2 vars\n  (`CLOUDFLARE_ACCOUNT_ID`, `CLOUDFLARE_R2_API_TOKEN` or `CLOUDFLARE_API_TOKEN`,\n  `R2_BUCKET`). Never print values; verify by name/length only.\n- Working model (verified 2026-09): `GROQ_MODEL` must be a model with solid\n  tool-calling. `qwen/qwen3.8-27b` emits boolean `direction` (Groq 400s every\n  batch); `openai/gpt-oss-120b` answers text-only. Prefer the strongest\n  tool-caller the key can list via `GET /openai/v1/models`.\n\n## 1. Pick the prospect\n\n```powershell\n# Galway example — needs website + videoStatus NONE (or use --force)\nbun C:\\Users\\0\\AppData\\Local\\Temp\\opencode\\galway.mjs\n```\n\n## 2. Run (forced + cropped, full log to file)\n\n```powershell\ncd C:\\Users\\0\\.buzz\\REPOS\\ui-kit\\apps\\web\nbun run video:agency-prospect -- <slug> --force --crop > C:\\Users\\0\\AppData\\Local\\Temp\\opencode\\vidrun.log 2>&1\n```\n\nWhat a healthy run prints, in order: `[preflight] model ok` →\n`template-refs pack=tint` → `Launching Camoufox` → `document classified as:\nreal_content` → `discovery` → `first response received: N tool calls` →\n`MODEL READY - STARTING CAP RECORDING` → per-step `TOOL CALL: scroll/click/\nnavigate` → `export complete` → `crop complete` → `Uploaded to R2:` →\n`Updated Twenty videoUrl` → `<slug>: ATTACHED source=https://...r2.dev/...mp4`.\n\n## 3. Read failures (they name themselves)\n\n| Log line | Meaning | Fix |\n|---|---|---|\n| `[preflight] FATAL` | Bad/missing model key or model, before anything launches (exit 4) | Fix `GROQ_*` env, don't retry |\n| `tool_use_failed ... direction ... boolean` | Model can't emit valid tool calls | Switch `GROQ_MODEL` to a stronger tool-caller |\n| `first response was text-only` → `FAILED` | Model refused to act | Same as above |\n| `cursor self-test failed: mouse.move hung` → `FAILED` | Humanize broken (GPU/locked desktop) | Free the desktop or repair Camoufox; do not re-sweep blindly |\n| `no tool calls executed` → `FAILED` | Stream died with zero motion | Read the 400/error above it |\n| Instant `ATTACHED` on near-empty files | Pre-loud-gate behavior; if seen again the gates regressed | Check `has_no_tool_motion`/`is_cursor_dead` guards |\n\nExit codes: `0` ok (or honest degrade), `2` URL unreachable (don't retry),\n`3` retryable agent failure, `4` config error (never retry).\n\n## 4. Verify the video\n\n1. Files: `apps/web/public/robovsl/<name>-<industry>-website/` —\n   `raw.mp4` tens of MB, `cropped.mp4` non-trivial, `cursor.json` with moves,\n   `.state-trace.json` present.\n2. Twenty: prospect `videoUrl.primaryLinkUrl` is the new R2 URL,\n   `videoStatus` goes RECORDING → ATTACHED (FAILED + `videoError` otherwise).\n3. Page: the prospect's funnel/template page serves the R2 MP4 (not a\n   previous upload — compare timestamps).\n\n## 5. Flags\n\n`--force` re-runs finished prospects. `--crop` adds the window-cropped mp4\n(preferred — fullscreen shows desktop chrome). `--fast-start` (default on)\ntrims the real-site preamble; `--no-fast-start` restores it.\n`--hide-native-cursor` hides the red dot; `--no-cursor` skips tracking\nentirely (never for deliverables).\n\n## Known incidents\n\n- **2026-09-11 ~04:00: Vercel production paused (402).** The\n  `listeningkit-frontend-web` production deployment started serving\n  `402 \"This deployment is temporarily paused\"` on `/` and\n  `/autobody/:id`. Any VSL run in this window records the generated-site\n  half against a dead page (verified in frames: real site fine, generated\n  site = paused page). The Dentmaster take from this window must be\n  re-recorded once production is unpaused/redeployed. Lesson: before any\n  run, curl the generated URL and require HTTP 200 — a paused frontend\n  poisons the whole take.",
});
