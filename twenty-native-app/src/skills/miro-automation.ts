import { defineSkill } from 'twenty-sdk/define';

import { MIRO_AUTOMATION_SKILL_UNIVERSAL_IDENTIFIER } from 'src/constants/universal-identifiers';

// Ported from local agent skill: miro-automation/SKILL.md
export default defineSkill({
  universalIdentifier: MIRO_AUTOMATION_SKILL_UNIVERSAL_IDENTIFIER,
  name: 'miro-automation',
  label: 'Miro Automation',
  description: 'Automate Miro tasks via the Composio plugin (Tool Router meta-tools): boards, items, sticky notes, frames, tags, connectors, groups, sharing, members. Always call COMPOSIO_SEARCH_TOOLS first to discover the current tool slugs and schemas.',
  content: "# Miro Automation via Composio Plugin\n\nAutomate Miro whiteboard operations through Composio's Miro toolkit (73 tools),\naccessed via the Composio plugin's Tool Router meta-tools in Cursor. No external\nMCP server URL or API key is needed when the Composio plugin is installed.\n\n## Prerequisites\n\n- The Composio plugin is installed in Cursor (namespace `plugin-composio-composio`)\n- An active Miro connection (OAuth2) via `COMPOSIO_MANAGE_CONNECTIONS`\n- **Always call `COMPOSIO_SEARCH_TOOLS` first** to discover current tool slugs and\n  fetch full input schemas before executing any Miro tool\n\n> **Migration note**: This skill was updated from the deprecated Rube MCP\n> (`rube.app/mcp`, `RUBE_SEARCH_TOOLS`, `RUBE_MANAGE_CONNECTIONS`) to the Composio\n> plugin's session-based Tool Router. The old `RUBE_*` tools no longer exist.\n> Tool slugs were also renamed (e.g. `MIRO_GET_BOARDS2` → `MIRO_GET_BOARDS_V2`,\n> `MIRO_GET_CONNECTORS2` → `MIRO_GET_CONNECTORS`, `MIRO_CREATE_FRAME_ITEM2` →\n> `MIRO_CREATE_FRAME_ITEM`).\n\n## Meta-Tool Workflow\n\nAll Miro operations flow through these Composio meta-tools. The Miro tools\nthemselves are never called directly — they are executed via the meta-tools.\n\n### 1. Discover tools — `COMPOSIO_SEARCH_TOOLS`\n\nAlways the first call. Returns tool slugs, input schemas, an execution plan,\ncommon pitfalls, and the current connection status for Miro.\n\n```\nqueries: [\n  { use_case: \"list all boards in miro toolkit\" }\n]\nsession: { generate_id: true }\nsearch_strategy: \"auto\"\n```\n\n- Returns ~4–6 tools per query with `tool_slug`, `description`, `input_schema`\n  (or `schemaRef` if a separate schema fetch is required).\n- If a `schemaRef` is returned instead of `input_schema`, fetch the full schema\n  via `COMPOSIO_GET_TOOL_SCHEMAS` before executing.\n- If `search_strategy: \"auto\"` returns a plan that is missing an expected tool,\n  retry the same query with `search_strategy: \"tool_search\"`.\n- The response includes a `session_id` — reuse it in all subsequent meta-tool\n  calls in the same workflow.\n- If the user pivots to a different task mid-chat, call this again with\n  `session: { generate_id: true }` to start a fresh session.\n\n### 2. Connect Miro — `COMPOSIO_MANAGE_CONNECTIONS`\n\nOnly needed if `COMPOSIO_SEARCH_TOOLS` reports no ACTIVE connection for `miro`.\n\n```\ntoolkits: [{ name: \"miro\", action: \"add\" }]\nsession_id: \"<from COMPOSIO_SEARCH_TOOLS>\"\n```\n\n- Returns a `redirect_url` — show it to the user as a formatted Markdown link.\n- Immediately after, call `COMPOSIO_WAIT_FOR_CONNECTIONS` to poll until active.\n- Supports multiple accounts per toolkit (use `alias` to distinguish, e.g.\n  \"work\" vs \"personal\"). Use `action: \"list\"` to see existing accounts.\n\n### 3. Wait for auth — `COMPOSIO_WAIT_FOR_CONNECTIONS`\n\n```\ntoolkits: [\"miro\"]\nmode: \"any\"   # or \"all\" when connecting multiple toolkits\nsession_id: \"<from COMPOSIO_SEARCH_TOOLS>\"\n```\n\nPolls until the connection reaches a terminal state (`ACTIVE` or `FAILED`).\n\n### 4. Fetch full schemas — `COMPOSIO_GET_TOOL_SCHEMAS`\n\nOnly needed when `COMPOSIO_SEARCH_TOOLS` returned a `schemaRef` instead of an\ninline `input_schema`.\n\n```\ntool_slugs: [\"MIRO_CREATE_STICKY_NOTE_ITEM\", \"MIRO_GET_BOARD_ITEMS\"]\ninclude: [\"input_schema\"]\nsession_id: \"<from COMPOSIO_SEARCH_TOOLS>\"\n```\n\n### 5. Execute Miro tools — `COMPOSIO_MULTI_EXECUTE_TOOL`\n\nExecutes up to 50 logically independent tools in parallel. Tools that depend on\neach other's outputs (e.g. \"find a board, then add a sticky note to it\") must\nrun in separate sequential calls.\n\n```\ntools: [\n  { tool_slug: \"MIRO_CREATE_BOARD\", arguments: { name: \"Sprint Planning\" } }\n]\nsync_response_to_workbench: false\nsession_id: \"<from COMPOSIO_SEARCH_TOOLS>\"\n```\n\n- Set `sync_response_to_workbench: true` when the response may be large or will\n  be needed later for scripting (e.g. listing hundreds of board items).\n- When multiple Miro accounts are connected, pass `account: \"<alias or id>\"` on\n  each tool entry.\n- **Never invent tool slugs or argument field names.** Always use slugs and\n  schemas returned by `COMPOSIO_SEARCH_TOOLS` / `COMPOSIO_GET_TOOL_SCHEMAS`.\n\n## Core Workflows\n\n### 1. List and Browse Boards\n\n**When to use**: User wants to find boards or get board details.\n\n**Sequence**:\n1. `COMPOSIO_SEARCH_TOOLS` with use_case `\"list all boards in miro toolkit\"`\n2. If no ACTIVE connection → `COMPOSIO_MANAGE_CONNECTIONS` + `COMPOSIO_WAIT_FOR_CONNECTIONS`\n3. `COMPOSIO_MULTI_EXECUTE_TOOL` with `MIRO_GET_BOARDS_V2`\n4. (Optional) `MIRO_GET_SPECIFIC_BOARD` with `board_id` for details\n\n**Key parameters** (`MIRO_GET_BOARDS_V2`):\n- `query`: Search term to filter boards by name\n- `sort`: `'default'`, `'last_modified'`, `'last_opened'`, `'last_created'`, `'alphabetically'`\n- `limit`: Number of results per page (max 50)\n- `offset`: Pagination offset (offset-based, not cursor-based)\n\n**Pitfalls**:\n- Maximum 50 boards per page; iterate with `offset` for the full list.\n- Board IDs are long alphanumeric strings; always resolve by search first.\n\n### 2. Create Boards and Items\n\n**When to use**: User wants to create a new board or add items to an existing board.\n\n**Sequence**:\n1. Resolve target `board_id` via `MIRO_GET_BOARDS_V2` (or create with `MIRO_CREATE_BOARD`)\n2. Create items: `MIRO_CREATE_STICKY_NOTE_ITEM`, `MIRO_CREATE_FRAME_ITEM`,\n   `MIRO_CREATE_SHAPE_ITEM`, `MIRO_CREATE_TEXT_ITEM`, `MIRO_CREATE_CARD_ITEM`,\n   `MIRO_CREATE_APP_CARD_ITEM`, `MIRO_CREATE_DOCUMENT_ITEM`, `MIRO_CREATE_EMBED_ITEM`,\n   or `MIRO_CREATE_ITEMS_IN_BULK`\n\n**Key parameters**:\n- `board_id`: Target board ID (required for all item creation)\n- `data`: Content object — e.g. `{ content: \"Note text\" }` for sticky notes\n- `style`: Styling object — e.g. `{ fillColor: \"#FF0000\" }` for sticky note color\n- `position`: `{ x, y }` coordinates (board origin is center; +X right, +Y down)\n- `geometry`: `{ width, height }` — required for frames\n\n**Pitfalls**:\n- `board_id` is required for ALL item operations; resolve via `MIRO_GET_BOARDS_V2` first.\n- Sticky note colors use hex codes in the `fillColor` field.\n- Position defaults to `(0,0)` if omitted; items may overlap.\n- Bulk create has a maximum items-per-request limit — check the schema returned\n  by `COMPOSIO_GET_TOOL_SCHEMAS`.\n\n### 3. Browse and Manage Board Items\n\n**When to use**: User wants to view, find, or organize items on a board.\n\n**Sequence**:\n1. `MIRO_GET_BOARD_ITEMS` — list all items on a board\n2. (Optional) `MIRO_GET_CONNECTORS` — list connections between items\n3. (Optional) `MIRO_GET_SPECIFIC_ITEM` — get a single item by ID\n4. (Optional) Per-type getters: `MIRO_GET_STICKY_NOTE_ITEM`, `MIRO_GET_SHAPE_ITEM`,\n   `MIRO_GET_TEXT_ITEM`, `MIRO_GET_FRAME_ITEM`, `MIRO_GET_CARD_ITEM`,\n   `MIRO_GET_APP_CARD_ITEM_2`, `MIRO_GET_DOCUMENT_ITEM`, `MIRO_GET_EMBED_ITEM`,\n   `MIRO_GET_IMAGE_ITEM`\n\n**Key parameters** (`MIRO_GET_BOARD_ITEMS`):\n- `board_id`: Target board ID (required)\n- `type`: Filter by item type (`'sticky_note'`, `'shape'`, `'text'`, `'frame'`,\n  `'image'`, `'card'`, `'app_card'`, `'document'`, `'embed'`)\n- `limit`: Number of items per page\n- `cursor`: Pagination cursor from previous response (cursor-based)\n\n**Pitfalls**:\n- Board items use **cursor-based** pagination (follow `cursor` until absent);\n  boards list uses **offset-based** pagination. Do not mix them.\n- Item type values must match Miro's predefined types exactly.\n- Connectors are separate from items — use `MIRO_GET_CONNECTORS` for\n  relationship data.\n\n### 4. Share and Collaborate on Boards\n\n**When to use**: User wants to share a board with team members or manage access.\n\n**Sequence**:\n1. `MIRO_GET_BOARDS_V2` — find the board to share\n2. `MIRO_SHARE_BOARD` — invite users by email\n3. (Optional) `MIRO_GET_BOARD_MEMBERS` — verify current members\n4. (Optional) `MIRO_GET_SPECIFIC_BOARD_MEMBER` — details for one member\n5. (Optional) `MIRO_UPDATE_BOARD_MEMBER` — change a member's role\n\n**Key parameters** (`MIRO_SHARE_BOARD`):\n- `board_id`: Board to share (required)\n- `emails`: Array of email addresses to invite\n- `role`: `'viewer'`, `'commenter'`, or `'editor'` (case-sensitive)\n- `message`: Optional invitation message\n\n**Pitfalls**:\n- Invalid email addresses cause the entire request to fail.\n- Sharing with users outside the organization may require admin approval.\n\n### 5. Create Visual Connections\n\n**When to use**: User wants to connect items on a board with lines or arrows.\n\n**Sequence**:\n1. `MIRO_GET_BOARD_ITEMS` — find source and target item IDs\n2. `MIRO_CREATE_CONNECTOR` — create the connector\n3. (Optional) `MIRO_GET_CONNECTORS` or `MIRO_GET_CONNECTOR` — view connections\n4. (Optional) `MIRO_UPDATE_CONNECTOR` — modify an existing connector\n5. (Optional) `MIRO_DELETE_CONNECTOR` — remove a connector\n\n**Key parameters** (`MIRO_CREATE_CONNECTOR`):\n- `board_id`: Target board ID\n- `startItem`: `{ id }` of the source item\n- `endItem`: `{ id }` of the target item\n- `style`: Connector style (line type, color, arrows)\n\n**Pitfalls**:\n- Both start and end items must exist on the same board.\n- Self-referencing connections (same start and end) are not allowed.\n\n### 6. Tags\n\n**When to use**: User wants to categorize items with tags.\n\n**Sequence**:\n1. `MIRO_LIST_BOARD_TAGS` — see existing tags on a board\n2. `MIRO_CREATE_TAG` — create a new tag\n3. `MIRO_ATTACH_TAG_TO_ITEM` — attach a tag to an item\n4. `MIRO_GET_ITEM_TAGS` — see tags on a specific item\n5. `MIRO_UPDATE_TAG` / `MIRO_DELETE_TAG` / `MIRO_GET_TAG` — manage individual tags\n\n### 7. Groups\n\n**When to use**: User wants to group multiple items together on a board.\n\n**Sequence**:\n1. `MIRO_GET_ALL_GROUPS` — list all groups on a board (cursor-based pagination)\n2. `MIRO_CREATE_GROUP` — create a new group from multiple items\n3. `MIRO_GET_GROUP_BY_ID` — get a specific group\n4. `MIRO_UPDATE_GROUP` / `MIRO_DELETE_GROUP` — manage groups\n\n## All 73 Miro Tools (grouped)\n\n> These are the human-readable names. The actual `tool_slug` values are\n> SCREAMING_SNAKE_CASE and may include version suffixes (e.g. `_V2`, `_2`).\n> **Always discover real slugs via `COMPOSIO_SEARCH_TOOLS` before executing.**\n\n### Boards\n- Create Board, Update Board, Get Specific Board, Get Boards V2, Share Board,\n  Get Organization Context, Get oEmbed Data\n\n### Items — Create\n- Create App Card Item, Create Card Item, Create Document Item, Create Document\n  Item Using File From Device, Create Embed Item, Create Frame Item, Create Image\n  Item Using Local File, Create Items in Bulk, Create Mind Map Node (Experimental),\n  Create Shape Item, Create Sticky Note Item, Create Text Item\n\n### Items — Read\n- Get App Card Item 2, Get Board Items, Get Card Item, Get Document Item,\n  Get Embed Item, Get Frame Item, Get Image Item, Get Mind Map Node, Get Mind\n  Map Nodes (Experimental), Get Shape Item, Get Specific Item, Get Sticky Note\n  Item, Get Text Item\n\n### Items — Update\n- Update App Card Item 2, Update Card Item, Update Document Item, Update Embed\n  Item, Update Frame Item, Update Image Item, Update Item Position or Parent,\n  Update Shape Item, Update Sticky Note Item, Update Text Item\n\n### Items — Delete\n- Delete App Card Item, Delete Card Item, Delete Document Item, Delete Embed\n  Item, Delete Frame Item, Delete Image Item, Delete Item, Delete Mind Map Node\n  (Experimental), Delete Shape Item, Delete Sticky Note Item, Delete Text Item\n\n### Connectors\n- Create Connector, Get Connector, Get Connectors, Update Connector,\n  Delete Connector\n\n### Tags\n- Attach Tag To Item, Create Tag, Delete Tag, Get Item Tags, Get Tag, List Board\n  Tags, Update Tag\n\n### Groups\n- Create Group, Delete Group, Get All Groups, Get Group By ID, Update Group\n\n### Members\n- Get Board Members, Get Specific Board Member, Update Board Member\n\n## Common Patterns\n\n### ID Resolution\n\n**Board name → Board ID**:\n1. `COMPOSIO_SEARCH_TOOLS` → discover `MIRO_GET_BOARDS_V2`\n2. `COMPOSIO_MULTI_EXECUTE_TOOL` with `MIRO_GET_BOARDS_V2`, `query: \"<board name>\"`\n3. Find the board in the response, extract its `id`\n\n**Item lookup on board**:\n1. `COMPOSIO_MULTI_EXECUTE_TOOL` with `MIRO_GET_BOARD_ITEMS`, `board_id`, optional `type` filter\n2. Find the item by content or position, extract its `id`\n\n### Pagination\n\n| Resource | Method |\n|----------|--------|\n| Boards (`MIRO_GET_BOARDS_V2`) | `offset` + `limit` (offset-based) |\n| Board items (`MIRO_GET_BOARD_ITEMS`) | `cursor` + `limit` (cursor-based) |\n| Groups (`MIRO_GET_ALL_GROUPS`) | `cursor` + `limit` (cursor-based) |\n\nContinue until no more results or the cursor is absent.\n\n### Coordinate System\n\n- Board origin `(0,0)` is at the center\n- Positive X is right, positive Y is down\n- Items are positioned by their center point\n- `position: { x: 0, y: 0 }` places an item at the board center\n- Frames define bounded areas; items inside inherit the frame's position\n\n## Known Pitfalls\n\n**Board IDs**:\n- Required for virtually all item, connector, tag, and group operations.\n- Always resolve board names to IDs via `MIRO_GET_BOARDS_V2` first.\n- Never hardcode board IDs — they vary by account.\n\n**Item Creation**:\n- Each item type has different required fields. Sticky notes need\n  `data.content`; frames need `geometry.width` and `geometry.height`.\n- Use `MIRO_CREATE_ITEMS_IN_BULK` for multiple items (respects rate limits).\n\n**Rate Limits**:\n- Miro API has rate limits per token.\n- Prefer bulk operations over individual item creation.\n\n**Response Parsing**:\n- Response data may be nested under a `data` key.\n- Parse defensively; optional fields may be absent.\n- For large responses, set `sync_response_to_workbench: true` on\n  `COMPOSIO_MULTI_EXECUTE_TOOL` and process in the workbench.\n\n**Schema changes**:\n- Composio updates tool schemas automatically. Never assume a field exists —\n  always check the schema returned by `COMPOSIO_SEARCH_TOOLS` or\n  `COMPOSIO_GET_TOOL_SCHEMAS` for the current shape.\n\n## Quick Reference\n\n| Task | Tool (human name) | Key Params |\n|------|-------------------|------------|\n| List boards | Get Boards V2 | query, sort, limit, offset |\n| Get board | Get Specific Board | board_id |\n| Create board | Create Board | name, description |\n| Update board | Update Board | board_id, fields to update |\n| Add sticky note | Create Sticky Note Item | board_id, data, style, position |\n| Add frame | Create Frame Item | board_id, data, geometry, position |\n| Bulk add items | Create Items in Bulk | board_id, items |\n| Get board items | Get Board Items | board_id, type, cursor |\n| Share board | Share Board | board_id, emails, role |\n| Get members | Get Board Members | board_id |\n| Create connector | Create Connector | board_id, startItem, endItem, style |\n| Get connectors | Get Connectors | board_id |\n| List tags | List Board Tags | board_id |\n| Create tag | Create Tag | board_id, name, color |\n| List groups | Get All Groups | board_id, cursor |\n\n## When to Use\n\nThis skill is applicable whenever the user wants to automate Miro whiteboard\noperations — creating boards, adding/managing items (sticky notes, shapes, text,\nframes, cards, documents, embeds, images), connecting items, tagging, grouping,\nsharing boards, or managing members — through the Composio plugin.\n\n<!-- AGI-INTEGRATION-START -->\n\n## AGI Framework Integration\n\n> **Adapted for [@techwavedev/agi-agent-kit](https://www.npmjs.com/package/@techwavedev/agi-agent-kit)**\n> Original source: [antigravity-awesome-skills](https://github.com/sickn33/antigravity-awesome-skills)\n\n### Memory-First Protocol\n\nCache workflow configurations and automation patterns. Retrieve prior pipeline designs to avoid re-building similar flows from scratch.\n\n```bash\n# Check for prior workflow/automation context before starting\npython3 execution/memory_manager.py auto --query \"automation patterns and workflow configurations for Miro Automation\"\n```\n\n### Storing Results\n\nAfter completing work, store workflow/automation decisions for future sessions:\n\n```bash\npython3 execution/memory_manager.py store \\\n  --content \"Workflow: automated data pipeline with retry logic, dead-letter queue, and Slack alerts on failure\" \\\n  --type technical --project <project> \\\n  --tags miro-automation workflow\n```\n\n### Multi-Agent Collaboration\n\nShare workflow state with other agents so they can trigger, monitor, or extend the automation.\n\n```bash\npython3 execution/cross_agent_context.py store \\\n  --agent \"<your-agent>\" \\\n  --action \"Workflow automation deployed — pipeline processing 1000+ events/day with 99.9% success rate\" \\\n  --project <project>\n```\n\n### Playbook Engine\n\nCombine this skill with others using the Playbook Engine (`execution/workflow_engine.py`) for guided multi-step automation with progress tracking.\n\n<!-- AGI-INTEGRATION-END -->",
});
