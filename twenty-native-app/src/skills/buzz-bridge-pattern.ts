import { defineSkill } from 'twenty-sdk/define';

import { BUZZ_BRIDGE_PATTERN_SKILL_UNIVERSAL_IDENTIFIER } from 'src/constants/universal-identifiers';

// Ported from local agent skill: buzz-bridge-pattern/SKILL.md
export default defineSkill({
  universalIdentifier: BUZZ_BRIDGE_PATTERN_SKILL_UNIVERSAL_IDENTIFIER,
  name: 'buzz-bridge-pattern',
  label: 'Buzz Bridge Pattern',
  description: 'Canonical reference for posting to a Buzz channel from a backend service using NIP-98 HTTP auth + nostr-tools, without using the Buzz CLI. Load when the task involves posting to a Buzz channel from a bridge, pipeline, or backend service.',
  content: "# Buzz Bridge Pattern (NIP-98 + nostr-tools)\r\n\r\nBackend services post to a Buzz channel by importing `nostr-tools` (pure-JS Nostr library), constructing a kind-9 event, signing it with a Nostr private key via `finalizeEvent`, authenticating with NIP-98 HTTP auth (kind 27235), and POSTing directly to the relay's `/events` endpoint.\r\n\r\n## Why NOT the Buzz CLI\r\n\r\nThe Buzz CLI (`buzz messages send`) is for interactive use, not for backend bridges:\r\n\r\n1. **Container-hostile** — compiled Rust binary adds build time, image size, system dependency.\r\n2. **Process overhead** — every invocation forks a subprocess vs in-process HTTP call.\r\n3. **Channel membership** — CLI posts as the calling user; NIP-98 signs with a fixed key.\r\n4. **Proven failure** — the v1 Reddit bridge shelled out to the CLI and was rejected. The working GitHub bridge (v2) uses NIP-98.\r\n\r\n## Dependencies\r\n\r\n```bash\r\nnpm install nostr-tools\r\n```\r\n\r\nThat's it. No buzz binary, no SDK, no platform-specific install.\r\n\r\n## How it works\r\n\r\n1. Bridge receives a webhook payload (GitHub, Reddit, any HTTP source).\r\n2. Constructs a Nostr **kind-9** event with:\r\n   - `content`: message text\r\n   - `tags`: [[\"h\", \"<channel-uuid>\"]] to target the channel\r\n   - `created_at`: current Unix timestamp\r\n3. Signs with `finalizeEvent(event, skBytes)` using a Nostr private key (hex or nsec1).\r\n4. Builds a **NIP-98 HTTP auth event** (kind 27235) with u/method/nonce tags, signs it, base64-encodes.\r\n5. Sends `POST <relay-url>/events` with `Authorization: Nostr <base64>` header and the kind-9 event as body.\r\n\r\n## Key details\r\n\r\n- **@-mention neutralization**: replace `@` with `@\\u200B` (zero-width space) before posting to prevent pings.\r\n- **Nonce**: each NIP-98 auth uses `randomUUID()` to prevent replay attacks.\r\n- **Payload hash**: SHA-256 of the body included in NIP-98 auth tags to bind auth to the payload.\r\n- **Key formats**: accepts both hex (64-char) and nsec1 (Bech32) private keys.\r\n\r\n## Environment variables\r\n\r\n| Variable | Required | Purpose |\r\n|----------|----------|---------|\r\n| `MATT_PRIVATE_KEY` or `BUZZ_PRIVATE_KEY` | Yes | Nostr private key for signing (nsec1 or hex) |\r\n| `BUZZ_RELAY_URL` | Yes | Relay WebSocket URL |\r\n\r\n## Use cases\r\n\r\n| Use case | Approach |\r\n|----------|----------|\r\n| Bridge server (HTTP service receiving webhooks) | **NIP-98 + nostr-tools** |\r\n| Pipeline server (Twenty workflow → Buzz channel) | **NIP-98 + nostr-tools** |\r\n| Interactive agent running in Buzz Desktop | Buzz CLI via `buzz messages send` |\r\n| One-off channel message from a script | Either — prefer bridge pattern |\r\n\r\n## Related docs\r\n\r\n- `docs/okf/buzz/bridge-pattern.md` — canonical reference in InferenceSaver repo\r\n- `docs/okf/buzz/index.md` — Buzz Platform Integration\r\n- `GUIDES/GITHUB_SOCIAL_LISTENER_BRIDGE_V2.md` — reference implementation\r\n- `REPOS/inferencesaver/scripts/buzz-lib.mjs` — reusable buzz lib in the repo",
});
